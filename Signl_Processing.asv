clc;
clear;
t=0:0.001:1;
f=50;
signal = sin(2*pi*f*t);
noise=.5* randn(size(t));
noisy_signal=signal+noise;
fs=1000;
fc=100;
order=50;
b=fir1order,fc/fs/2(; ...)()
    filtered signal=filter(b,1,noisy_signal);
figure;
subplot(3,1,1)
;
plot(t(1:200))